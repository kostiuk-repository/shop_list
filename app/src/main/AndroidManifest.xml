<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.olga.shoplist">

    <application
        android:allowBackup="true"
        android:name="com.olga.shoplist.ShopListApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:screenOrientation="portrait"
            android:name=".viewlayer.activitys.ShopCartListActivity"
            android:label="@string/title_activity_shop_cart_list"
            android:theme="@style/AppTheme"></activity>
        <activity
            android:screenOrientation="portrait"
            android:name=".viewlayer.activitys.StartActivity"
            android:windowSoftInputMode="adjustPan"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>